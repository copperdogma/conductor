# Alignment Entries

Create one file per substantive comparison pass using a stable slug and a
three-digit id, for example:

- `align-001-infrastructure-skill-baseline.md`

Each entry should capture:

- source surfaces compared
- target projects checked
- key differences
- what should sync
- what should stay local
- what needs a human decision

